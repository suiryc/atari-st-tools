atari-st-tools {

  check {
    boot-sector = true
  }

  warn {
    unknown-format = true
  }

  zip {
    allowed-extra = [ "(?i).*(?:inf|read).*\\.txt" ]
    charset = "ISO-8859-1"
  }

  output {
    root = "~/dev/atari/deduplicate"
    preferred = "preferred"
    others = "others"
    converted = "converted"
  }

}
